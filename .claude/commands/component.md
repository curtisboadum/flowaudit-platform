# Scaffold React Component

Create a new React component.

## Arguments
$ARGUMENTS

## Steps

1. Determine the component name, type (server/client), and directory from the arguments or ask the user.
2. Create the component file following the `component` skill patterns.
3. Define the props interface.
4. Implement the component with Tailwind styling.
5. Run `pnpm typecheck` to verify.
6. Update `docs/tracking/change_log.md`.
